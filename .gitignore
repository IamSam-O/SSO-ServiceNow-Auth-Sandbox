# Secrets
!.gitkeep

*.env
sso-tunnel/ngrok/.env
sso-tunnel/ngrok/ngrok.yml
sso-infrastructure/data
# Cloudflare tunnel credentials — generated on first run
sso-tunnel/cloudflare/data/

# Cloudflare routes — contains your domain
sso-tunnel/cloudflare/routes.conf

# PowerShell scripts — local helpers, not committed
*.ps1

# OS
.DS_Store
Thumbs.db
desktop.ini
# Editors
.vscode/
.idea/
*.swp
*~